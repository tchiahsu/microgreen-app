export type Profile = {
    first_name: string;
    last_name: string;
    email: string;
    password: string;
}

export type UserProfile = {
    employee_id: number;
    first_name: string;
    last_name: string;
    email: string;
};